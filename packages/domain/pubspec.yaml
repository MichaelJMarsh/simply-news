name: domain
description: "Core layer defining business logic, models and abstract repository interfaces."

publish_to: "none"

version: "1.0.0"

environment:
  sdk: ">=3.5.3 <4.0.0"
  flutter: ^3.24.3

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
